export class Photo {
  setId(id) {
    this.id = id;
  }

  setAlbumId(albumId) {
    this.albumId = albumId;
  }

  setTitle(title) {
    this.title = title;
  }

  setUrl(url) {
    this.url = url;
  }

  setThumbnailUrl(thumbnailUrl) {
    this.thumbnailUrl = thumbnailUrl;
  }
}
